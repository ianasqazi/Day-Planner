{
  "name": "day-planner",
  "version": "1.0.0",
  "description": "Simple Calendar Application that allows the user to save events for each hour of the day.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "moment": "^2.30.1"
  }
}
